#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>


int count = 0;


int grid[8][8];

void Select ()
{
	int row, col;
	for (row=0;row<8;row++) {
		for (col=0;col<8;col++) {
			if (grid[row][col] == -1) { break; ]
				
		}
		
	}

}


main()
{

  	
	move(0,0);
	
}

